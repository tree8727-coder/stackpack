# 오답노트

우리가 실제로 당한 사고만 적습니다. 일반론은 안 적습니다.

`check.py` 의 검사는 전부 여기서 나왔습니다. `vibe.yaml` 이 사람들의 AI에게
알려주는 것도 여기서 나옵니다. **이 파일이 이 저장소의 진짜 자산입니다** —
도구 목록은 누구나 만들 수 있지만, 당한 사고는 당한 사람만 가지고 있습니다.

번호는 예전 기록에서 이어집니다. 비어 있는 번호는 아직 안 옮겨 적은 것입니다.

---

## E5 — `.env` 가 저장소에 딸려 들어갔다

**있었던 일** — `.env` 가 git 에 추적되고 있었습니다. `.gitignore` 에 규칙이
없어서 만들자마자 그대로 커밋됐습니다.

**왜 나쁜가** — 커밋에 남으면 파일을 지워도 히스토리에 남습니다. 공개 저장소면
그대로 노출입니다.

**그래서** — `git rm --cached .env`, `.gitignore` 에 `.env` 와 `.env.*` 추가.
**이미 밀었으면 키를 새로 발급해야 합니다.** 지우는 걸로는 안 됩니다.

**안 잡히는 것** — `.env.example` 같은 템플릿은 일부러 커밋하는 파일이라
검사에서 뺐습니다. 거기에 진짜 값을 적어 넣으면 안 잡힙니다.

검사: `check_env_tracked`, `check_gitignore`

---

## E7 — 정적 서빙이 소스 폴더를 통째로 열어 뒀다

**있었던 일** — 정적 파일 서빙이 프로젝트 루트를 가리키고 있었습니다.
소스·설정·백업이 그대로 읽혔습니다. **공개 터널을 열고 나서야 알았습니다.**

**그래서** — `public/` 같은 전용 폴더만 서빙합니다.

**안 잡히는 것** — 자바스크립트 계열 파일만 봅니다. 파이썬·PHP 쪽 같은 설정은
못 잡습니다.

검사: `check_static_root`

---

## E8 — 키와 계좌번호가 소스에 박혀 있었다

**있었던 일** — API 키가 소스에 그대로 있었습니다. 그리고 **로그인 안 한
사람에게 주최자 계좌번호가 통째로 보였습니다.**

**왜 나쁜가** — 한 번 밀면 회수가 안 됩니다. 크롤러가 공개 저장소를 먼저 봅니다.

**그래서** — 키는 환경변수로 빼고, 이미 밀었으면 폐기하고 새로 발급합니다.
계좌 같은 값은 저장소가 아니라 DB 로 옮기고, **응답에서 누구에게 보이는지**
다시 봅니다.

**안 잡히는 것** — 아는 모양의 키만 찾습니다. 처음 보는 형식은 못 잡습니다.
그리고 계좌번호 검사는 한때 날짜를 계좌로 잘못 읽었습니다 —
`123456-01-234567` 안의 `3456-01-23` 을 날짜로 봤던 것입니다.

검사: `check_secrets`

---

## E10 — 아무것도 지키지 않는 검사가 3주를 통과했다

**있었던 일** — 화면 검사를 `inner_text` 로 단언했습니다.
숨은 요소(`display:none`)는 `""` 를 돌려줍니다.
`"문구" not in ""` 는 **언제나 참**이라, 화면이 멈춰 있어도 검사는 통과했습니다.

**왜 나쁜가** — 통과하는 검사와 아무것도 안 보는 검사는 겉으로 구분되지
않습니다. 초록불만 보고 있게 됩니다.

**그래서** — `textContent` 로 DOM 을 직접 읽습니다. 그리고 **새 단언을 쓰면
그 단언이 지키려는 코드를 한 줄 주석 처리하고 한 번 돌려서, 진짜 터지는지
눈으로 봅니다.**

**안 잡히는 것** — `inner_text` 를 쓰는 곳만 찾습니다. 다른 방식으로 똑같이
헛도는 단언은 못 잡습니다.

검사: `check_hidden_assert`

---

## E13 — 무료 체험을 우리 설정으로 두 시간 만에 태웠다

**있었던 일** — Fly 무료 체험은 「VM 2시간 또는 7일 중 먼저 오는 것」입니다.
머신을 24시간 켜 두는 설정(`auto_stop_machines = off`, `min_machines_running = 1`)
으로 배포했습니다. **03:06 에 배포했고, 05시쯤 죽은 것을 14시간 뒤에 알았습니다.**

**그래서** — `auto_stop_machines` 를 켜고 `min_machines_running` 을 0 으로.
콜드 스타트가 싫어서 껐던 거라면, **그 대비책을 이미 만들어 놓고 껐던 것인지**
다시 봅니다.

**안 잡히는 것** — `fly.toml` 만 봅니다. 다른 서비스의 같은 사고는 못 잡습니다.
그리고 "죽은 걸 14시간 뒤에 알았다" 는 설정 문제가 아니라 **알림이 없던 문제**입니다.
그건 아직 아무 검사도 없습니다.

검사: `check_fly_free_tier`

---

## 라켓온 원본에서 옮겨 온 것

위 다섯 건은 `check.py` 가 **기계로 잡는** 사고입니다.
아래는 원본(`racketon/ERRORS.md`, 사고 24건)에서 **남의 프로젝트에도 그대로 나는 것만**
골라 옮긴 것입니다. 기계로는 못 잡아서 규칙으로만 알려줍니다.

전문은 `vibe.yaml` 에 있습니다 — 여기 또 적으면 두 벌이 되고, 두 벌은 갈라집니다(P5).

| 번호 | 무슨 일이 있었나 | vibe.yaml 항목 |
|---|---|---|
| **E3** | 수집이 0건으로 끝났는데 그 0건이 지난 데이터를 덮어썼다 | `빈결과가-좋은데이터를-덮음` |
| **E6** | 같은 줄에서 이름은 서버 값을, 실력은 사용자가 보낸 값을 썼다 | `한줄에-신뢰경계가-둘` |
| **E9** | 저장하기 전에 성공이라고 찍어서, 안 들어간 코드를 들어간 줄 알고 배포했다 | `성공을-먼저-찍음` |
| **E12** | "30일 무료" 라는 말을 날짜로 적어 다섯 군데에 퍼뜨렸는데 하루 만에 끝났다 | `들은말을-날짜로-굳힘` |
| **E19** | 바뀐 게 데이터가 아니라 규칙이었는데, 조기 반환이 그 길을 통째로 건너뛰었다 | `조기반환이-새규칙을-건너뜀` |
| **E22** | 모르는 것을 화면이 "없음" 이라고 단언하고 있었다 | `모르는걸-없음이라고-단언` |
| **E2** | 화면을 바꿨는데 검사를 안 고쳐서, 완료 표시가 무엇을 뜻하는지 아무도 모르게 됐다 | `완료표시가-기억에-붙음` |
| **E16** | 자리 채우기 전화번호가 "빈 번호 아님" 으로 통과했다 | `자리채우기값이-검증을-통과` |

    uv run vibe.py 보기 <항목>       # 하나 자세히

## 번호에 대해

번호는 원본(`racketon/ERRORS.md`)을 그대로 씁니다. 원본에는 사고가 **24건** 있고,
그중 남에게도 해당되는 것만 여기 있습니다. 나머지는 그 프로젝트에만 있는 일입니다
(공유누리 API 응답, 코트 중복 합치기 같은 것).

원본에 **E5 가 두 개** 있고 **E15 는 비어** 있습니다. 옮기면서 고치지 않았습니다 —
번호를 다시 매기면 원본과 대조가 안 됩니다.

**없는 걸 지어내지 않습니다.** 지어낸 사고가 하나 섞이는 순간 나머지도 못 믿게 됩니다.

## 새 사고 적는 법

[사용법 제출](../../issues/new?template=사용법-제출.yml) 로 보내주세요. 형식은 위와 같습니다.

1. **있었던 일** — 증상부터. 무엇을 보고 알았는지.
2. **왜 나쁜가** — 그냥 두면 어떻게 되는지.
3. **그래서** — 다음부터 뭘 다르게 하는지.
4. **안 잡히는 것** — 이렇게 해도 못 막는 경우. **이 칸이 빈 것은 안 받습니다.**
