# 스택팩 — 처음 여시는 분께

읽는 데 2분 걸립니다. 이대로만 하시면 됩니다.

---

## 1. 설치

압축을 푼 폴더에서 **`시작하기`** 파일을 두 번 클릭하세요. 그게 전부입니다.

검은 창이 뜨고 세 가지를 합니다.

1. `uv`(파이썬 실행기)가 없으면 깝니다 — 1~2분
2. 스택팩이 제대로 왔는지 확인합니다
3. **지금 PC에 무엇이 이미 깔려 있는지** 보여줍니다

> **도구를 마음대로 깔지 않습니다.** 3번까지만 하고 멈춥니다.
> 뭘 깔지는 확인하고 나서 직접 고르는 겁니다.

**"Windows의 PC 보호" 파란 창이 뜨면** — 처음 보는 파일이라 윈도우가 한 번 묻는 겁니다.
`추가 정보` → `실행`을 누르시면 됩니다.

---

## 2. 세 줄만 외우면 됩니다

설치가 끝나면 그 폴더에서 이 셋을 씁니다. 복사해서 붙여넣으세요.

```powershell
uv run build.py status content-factory
```
이 콤보에 뭐가 필요하고 뭐가 이미 있는지만 봅니다.

```powershell
uv run build.py install content-factory --skip-installed
```
없는 것만 **미리보기**. 아무것도 깔리지 않습니다.

```powershell
uv run build.py install content-factory --skip-installed --yes
```
여기서 실제로 깔립니다. **`--yes`를 붙였을 때만** 실행됩니다.

`content-factory` 자리에 다른 콤보 이름을 넣으면 됩니다.
콤보 목록은 `uv run build.py status`를 그냥 치면 전부 나옵니다.

---

## 3. 막혔을 때

**창을 통째로 캡처해서 카톡으로 보내주세요.**

- 카톡: `[[여기에 오픈채팅 링크 또는 아이디를 넣으세요]]`
- 평일 기준 하루 안에 답을 드립니다.

부분 캡처보다 **창 전체**가 훨씬 빠릅니다. 어디서 멈췄는지가 위쪽에 적혀 있어서요.
"안 돼요"만 보내주셔도 됩니다. 나머지는 물어보겠습니다.

---

## 4. 환불

**받으신 날부터 7일 안에는 이유를 묻지 않고 전액 돌려드립니다.**

카톡으로 "환불"이라고만 보내주세요. 계좌 알려주시면 그날 보내드립니다.
쓰신 만큼 빼거나 하지 않습니다. 전액입니다.

디지털 파일이라 회수할 방법이 없다는 걸 압니다. 그래도 이렇게 합니다.
아직 신뢰가 쌓이지 않은 물건을 파는 쪽이 감수할 몫이라고 생각합니다.

---

## 5. 이 돈이 무엇에 대한 값인지

**소개된 도구 31개는 전부 남이 만든 것이고, 전부 무료입니다.**
우리가 만든 게 아니고, 우리는 거기서 한 푼도 받지 않습니다.
전체 목록과 만든 곳은 함께 들어 있는 `출처.md`에 있습니다. 쓸 만했으면 그 저장소에 별을 눌러주세요.

도구 목록에는 값을 매기지 않았습니다. `stack.yaml`은 공개돼 있고,
**이 zip 안의 `stack.yaml`도 그 공개본과 같은 파일입니다.**

값을 매긴 건 우리가 쓴 것뿐입니다 — 내 PC에 뭐가 깔렸는지 대조하고, 미리보기를 기본값으로 두고,
콤보 설치 명령을 그때그때 만들어 내고, 스스로를 검사하는 부분. 그리고 막혔을 때 답하는 사람.

목록이 아니라 **그 목록이 내 PC에서 돌아가게 만드는 부분**을 사신 겁니다.

---

## 6. 이 파일은 뭘 하는 물건인가

도구 하나하나를 소개하는 카탈로그는 인터넷에 이미 많습니다. 이건 그게 아닙니다.

**읽는 카탈로그가 아니라 실행되는 카탈로그입니다.**

- `stack.yaml` 하나가 유일한 원본입니다. 도구·콤보·설치 명령이 전부 여기 있습니다.
- 콤보의 설치 명령은 **어디에도 적혀 있지 않습니다.** 들어 있는 도구들의 명령을
  순서대로 이어붙이고 중복을 지워서 그때그때 만듭니다. 그래서 도구를 하나 고치면
  그 도구가 든 콤보 전부가 같이 고쳐집니다.
- `uv run build.py selftest`가 그 정합성을 검사합니다. 안 맞으면 터집니다.

---

## 7. 업데이트

모델 가격과 성능은 계속 바뀝니다. 안 바뀐 채로 90일이 지나면 `selftest`가
**일부러 실패합니다** — 낡은 정보를 조용히 들고 있는 게 제일 나쁘다고 보기 때문입니다.

갱신본이 나오면 카톡으로 알려드립니다.

---

파는 사람: `[[이름 / 연락처를 넣으세요]]`
버전: `[[pack.py에 넣은 버전]]`
