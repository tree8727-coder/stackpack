# 같이 만드는 법

이 저장소는 **도구 카탈로그**이자 **사람들이 실제로 쓰는 방법의 기록**입니다.
코드보다 중요한 게 방법 쪽입니다.

## 입구는 하나입니다

사용법 제출은 [이슈 템플릿](../../issues/new?template=사용법-제출.yml) 으로만 받습니다.

인스타 DM, 카톡, 오프라인 모임 — 창구는 여러 개지만, 들은 이야기는
**본인이 이슈로 다시 내주셔야** 저장소에 실립니다.
남이 준 글을 공개 저장소에 올리려면 그 사람의 공개 동의가 필요하고,
동의를 확실히 남기는 자리가 이슈 템플릿 안의 체크박스이기 때문입니다.

제가 대신 받아 적어 올리지 않습니다. 한 번 예외를 두면 전부 예외가 됩니다.

## '최적'이라고 쓰지 않습니다

표본이 세 자리가 되기 전까지 이 저장소는 **어떤 방법도 "최적"이라고 부르지 않습니다.**

대신 이렇게 씁니다:

    이렇게 쓰는 사람 7명 · 최장 사용 3개월 · 출처 #12 #19 #23

근거 없이 '최적'을 주장하면 첫 반박 한 번에 저장소 전체의 신뢰가 날아갑니다.
도구 출처를 전부 밝히는 것(출처.md)과 같은 규율을 방법론에도 적용합니다.

## 제출은 이렇게 처리됩니다

1. 들어오면 `제보됨` 라벨이 붙습니다. **그 상태로 공개돼 있습니다** — 숨기지 않습니다.
2. 주 1회, 정해진 요일에 몰아서 봅니다. 그 외의 날엔 안 봅니다.
   (혼자 하는 일이고, 매일 보겠다고 하면 3주 뒤에 무너집니다.)
3. 직접 따라 해봅니다. 되면 `검증됨`, 안 되면 코멘트로 되물어봅니다.
4. **직접 못 돌려본 것은 `제보됨` 그대로 실립니다.** 윈도우 전용, 유료 API 필요,
   장비가 없음 — 이런 건 검증 도장을 찍지 않고 그 사실을 그대로 적습니다.
   가짜 검증 하나가 진짜 검증 100개를 죽입니다.
5. 어느 쪽이든 표기 이름과 이슈 번호가 함께 실립니다.

채택되지 않아도 이슈는 닫지 않고 남겨둡니다. 안 된 방법도 기록입니다.

## 단점이 없는 제출은 채택하지 않습니다

장점만 적힌 방법은 아직 충분히 안 써본 방법입니다.
"모르겠음"이라고 적는 건 괜찮습니다. 칸을 비우는 것만 안 됩니다.

## 코드 기여

축이 둘입니다. **`stack.yaml`**(설치되는 프로그램)과 **`vibe.yaml`**(내 프로젝트에 놓이는 파일).
섞지 마세요 — 모델이 다릅니다. `index.html` 이나 `skill/SKILL.md` 를 직접
고치지 마세요 — 생성물이라 다음 빌드에서 덮어씁니다.

PR 전에:

    uv run build.py selftest      # stack 축
    uv run vibe.py selftest       # vibe 축

라이선스는 MIT입니다. PR을 보내면 그 라이선스로 공개되는 데 동의한 것으로 봅니다.
