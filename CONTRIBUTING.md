# 같이 만드는 법

이 저장소는 **도구 카탈로그**이자 **사람들이 실제로 쓰는 방법의 기록**입니다.
코드보다 중요한 게 방법 쪽입니다.

## 제보는 두 종류입니다

| | 무엇 | 어떻게 판단하나 |
|---|---|---|
| **사고 제보** | «무슨 일이 났다» | 재현되나 → 기계로 잡히나 → 규칙으로 |
| **패턴 제보** | «이렇게 하니 결과가 달랐다» | **잰 게 있나** → 남에게도 나나 → 언제 안 통하나 |

패턴 제보는 사고가 아니라서 «있었던 일» 이 없습니다. 대신 **전후 비교**가 있어야
하고, **코드는 안 받습니다** — 구조만 받습니다. 회사 코드와 개인정보가 섞여
들어오는 것이 우리가 가장 피하고 싶은 일입니다.

> 잰 게 없어도 받습니다. 다만 «못 쟀다» 고 적혀 있어야 합니다.
> 안 잰 것을 잰 것처럼 적으면 그게 초록불입니다.

## 입구는 하나입니다

사용법 제출은 [이슈 템플릿](../../issues/new?template=사용법-제출.yml) 으로만 받습니다.

인스타 DM, 카톡, **한 달에 한 번 여는 당근 모임** — 창구는 여러 개지만, 들은 이야기는
**본인이 이슈로 다시 내주셔야** 저장소에 실립니다.
(모임에서는 그 자리에서 같이 이슈를 올려도 됩니다. 그게 제일 빠릅니다.)
남이 준 글을 공개 저장소에 올리려면 그 사람의 공개 동의가 필요하고,
동의를 확실히 남기는 자리가 이슈 템플릿 안의 체크박스이기 때문입니다.

제가 대신 받아 적어 올리지 않습니다. 한 번 예외를 두면 전부 예외가 됩니다.

## 받는 기준 · 버리는 기준 (근거: Bettenburg et al., FSE 2008)

개발자 466명을 조사한 [What Makes a Good Bug Report?](https://thomas-zimmermann.com/publications/files/bettenburg-fse-2008.pdf)
가 찾은 것은 **정보 불일치**입니다 — 받는 쪽이 가장 원하는 것(재현 절차·스택
트레이스·테스트 케이스)이 하필 **주는 쪽이 가장 만들기 어려운 것**입니다.
그리고 가장 심각한 문제 1위가 **재현 절차 오류**, 2위가 **불완전한 정보**였습니다.

그래서 우리는 **어려운 칸을 요구하지 않습니다.** 대신 이 셋만 봅니다.

| 받는다 | 왜 |
|---|---|
| **언제 그 일이 났나** (증상) | 없으면 격언입니다. AI 가 «지금이 그때» 를 못 알아봅니다 |
| **무엇을 보고 알았나** | 없으면 재현이 안 됩니다. 논문의 «심각도 1위» 가 이것입니다 |
| **고유한 것 하나** — 숫자·파일 이름·오류 문구 | 없으면 정보가 0 입니다 |

| 버린다 | 예 |
|---|---|
| 상태어만 있는 것 | "AI 쓰기 불편해요" — 감정은 니즈가 아닙니다 |
| 당연어만 있는 것 | "AI 가 실수했다" — 카페 글의 «커피» 와 같습니다 |
| 증상이 없는 것 | "코드는 블록으로 나눠 짜야 한다" — 맞는 말이지만 언제인지가 없습니다 |

「이렇게 해도 안 잡히는 것」 칸은 **우리가 채웁니다.** 제보자에게 그것까지
요구하면 아무도 못 냅니다 — 그게 논문이 말한 불일치입니다.

## 같은 사고가 또 오면 — **합치지 말고 셉니다**

같은 논문의 발견 하나 더: **중복 제보를 버리면 안 됩니다.** 중복이 오히려
정보를 더하고 해결을 앞당깁니다.

그래서 같은 사고가 다시 오면 새 항목을 만들지 않고 `evidence.users` 를 **1 올립니다.**
그리고 새 제보에만 있는 사실(다른 환경, 다른 증상, 다른 한계)을 본문에 더합니다.

이게 `users: 1` 을 진짜 표본으로 바꾸는 유일한 길입니다. **지금 23건이 전부
`users: 1` 입니다** — 여기가 이 저장소에서 제일 약한 자리입니다.

## 무엇을 싣고 무엇을 안 싣나 — 3단으로 거릅니다

**1단. 재현되나** — 그 사고를 다시 일으킬 수 있어야 합니다. 못 일으키면
`제보됨` 으로 그대로 실립니다. 지우지 않습니다. 검증 도장만 안 찍습니다.

**2단. 기계로 잡히나** — 잡히면 `check.py` 에 검사로 넣습니다.
**규칙보다 검사가 셉니다.** 규칙은 안 읽히지만 검사는 그 줄을 짚어줍니다.

**3단. 못 잡으면 규칙으로만** — `vibe.yaml` 에 넣어 AI 가 읽게 합니다.

그리고 모든 사고에 **「이렇게 해도 안 잡히는 것」을 반드시 적습니다.**
빈 칸이면 실을 수 없습니다 — `vibe.py selftest` 가 막습니다.

## 규칙을 늘리는 것은 공짜가 아닙니다

AI 에게 주는 지시는 많을수록 좋지 않습니다. **측정된 사실입니다.**

- 지시 밀도가 오르면 따르는 정확도가 임계점 이후 급락합니다
  ([IFScale, arXiv:2507.11538](https://arxiv.org/pdf/2507.11538))
- 제약 1단계 → 4단계에서 **10~15%p 하락**하고, 지시끼리 서로 싸웁니다
  ([arXiv:2407.03978](https://arxiv.org/pdf/2407.03978))
- 긴 맥락의 **가운데에 놓인 정보는 30% 이상 정확도가 떨어집니다**
  ([Lost in the Middle](https://arxiv.org/pdf/2311.09198))

그래서 세 가지를 지킵니다.

1. **규칙 파일에 넣는 것은 최대 12줄**입니다(`INDEX_MAX`). 넘치면 버리지 않고
   스킬로 보냅니다. 이 상수를 늘리려면 위보다 나은 근거가 있어야 하고,
   `selftest` 가 그걸 강제합니다.
2. **관문이 자동으로 막는 사고는 규칙에 안 적습니다.** 관문은 토큰도 지시 예산도
   0 인데 100% 확실합니다. 같은 것을 두 번 말하면 지시만 늘고 정확도는 떨어집니다.
3. **심각한 것이 먼저 옵니다.** 빈도만 보면 «드물지만 돈이 나가는» 사고가 아래로
   밀립니다. 드문 것과 안 중요한 것은 다릅니다.

## 나무위키가 아닙니다

글이 쌓이는 방식은 위키에서 가져왔습니다. **즉시 반영은 가져오지 않았습니다.**

여기 실린 글은 남의 AI 규칙 파일에 **자동으로** 들어가고, 자동 갱신까지
걸려 있습니다. 틀린 규칙 하나가 하루 만에 모든 설치본에 퍼집니다.
그래서 **제보는 열려 있고 반영은 닫혀 있습니다.**

## 값은 이름으로 갚습니다

전부 무료입니다. 돈은 안 받고 안 줍니다.

대신 실린 사고마다 **제보자 이름과 이슈 번호가 함께 박힙니다.**
`uvx stackpack 보기` 에서도, `ERRORS.md` 에서도 보입니다.
익명을 원하시면 "익명" 이라고 적어주시면 됩니다.

## '최적'이라고 쓰지 않습니다

표본이 세 자리가 되기 전까지 이 저장소는 **어떤 방법도 "최적"이라고 부르지 않습니다.**

대신 이렇게 씁니다:

    이렇게 쓰는 사람 7명 · 최장 사용 3개월 · 출처 #12 #19 #23

근거 없이 '최적'을 주장하면 첫 반박 한 번에 저장소 전체의 신뢰가 날아갑니다.
도구 출처를 전부 밝히는 것(출처.md)과 같은 규율을 방법론에도 적용합니다.

## 제출은 이렇게 처리됩니다

1. 들어오면 `제보됨` 라벨이 붙습니다. **그 상태로 공개돼 있습니다** — 숨기지 않습니다.
2. **한 달에 한 번** 몰아서 봅니다 — 당근 모임이 있는 달의 그 주에 함께 처리합니다.
   그 외의 날엔 안 봅니다. (혼자 하는 일이고, 매일 보겠다고 하면 3주 뒤에 무너집니다.)
3. 직접 따라 해봅니다. 되면 `검증됨`, 안 되면 코멘트로 되물어봅니다.
4. **직접 못 돌려본 것은 `제보됨` 그대로 실립니다.** 윈도우 전용, 유료 API 필요,
   장비가 없음 — 이런 건 검증 도장을 찍지 않고 그 사실을 그대로 적습니다.
   가짜 검증 하나가 진짜 검증 100개를 죽입니다.
5. 어느 쪽이든 표기 이름과 이슈 번호가 함께 실립니다.

채택되지 않아도 이슈는 닫지 않고 남겨둡니다. 안 된 방법도 기록입니다.

## 단점이 없는 제출은 채택하지 않습니다

장점만 적힌 방법은 아직 충분히 안 써본 방법입니다.
"모르겠음"이라고 적는 건 괜찮습니다. 칸을 비우는 것만 안 됩니다.

## 사고는 이렇게 적습니다

`ERRORS.md` 와 `vibe.yaml` 이 같은 형식입니다.

| 칸 | 무엇을 적나 |
|---|---|
| **증상** | 언제 이 사고가 나나. **「~하려 할 때」 같은 상황**으로 적습니다 |
| **있었던 일** | 실제로 무슨 일이 있었나. 날짜·숫자가 있으면 그대로 |
| **그래서** | 다음부터 뭘 다르게 하나 |
| **안 잡히는 것** | 이렇게 해도 못 막는 경우 |

증상이 제일 중요합니다. **AI 는 규칙을 못 알아보고 상황을 알아봅니다.**
"검사는 부숴보고 믿어라" 는 안 읽힙니다. "새 단언을 쓰고 통과를 확인할 때" 는
읽힙니다. `selftest` 가 증상 칸이 규칙문이면 막습니다.

AI 에게 가는 글은 **손으로 적지 않습니다.** `vibe.yaml` 의 칸에서 만들어냅니다.
두 벌이 되면 갈라지기 때문입니다(P5).

## 코드 기여

축이 둘입니다. **`stack.yaml`**(설치되는 프로그램)과 **`vibe.yaml`**(내 프로젝트에 놓이는 파일).
섞지 마세요 — 모델이 다릅니다. `index.html` 이나 `skill/SKILL.md` 를 직접
고치지 마세요 — 생성물이라 다음 빌드에서 덮어씁니다.

PR 전에:

    uv run build.py selftest      # stack 축
    uv run vibe.py selftest       # vibe 축

라이선스는 MIT입니다. PR을 보내면 그 라이선스로 공개되는 데 동의한 것으로 봅니다.
